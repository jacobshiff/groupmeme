<h2>Members of <%= current_group.title %></h2>

<ul>
  <% @users.each do |user| %>
    <li>
    <%= image_tag(user.avatar) %>
    <%= link_to user.username, user_path(user.username)%>
    </li>
  <% end %>
</ul>
