<div class="portfolio-wrapper">
<h1>Your Groups</h1>
<ul>
<% @groups.each do |group| %>
  <li>
    <%= link_to group.title, group_path(group.group_slug) %>
  </li>
<% end %>
</ul>
<%= link_to "Create A New Group", new_group_path, class: 'btn btn-primary' %>
</div>
