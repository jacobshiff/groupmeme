<div class="page">
  <article>
    <div class="page-header">
      <h1>Edit Group</h1>
      <p class="lead">Edit your organization's information.</p>
    </div>

    <div class="row"
    <div class="col-md-8">

      <%= form_for @group, url: group_path(@group.group_slug), html: {class: "form-horizontal"} do |f| %>
      <div class="form-group">
        <%= f.label :title, class: 'control-label' %>
        <div class="controls">
          <%= f.text_field :title %>
        </div><br>
        <div id="target">
          <%= image_tag @group.image, class: "thumb", style: "width:30%"%>
        </div>
        <div class="sub-text">
          <%= f.label :image, "Customize your group photo" %>
          <%= f.file_field(:image, accept: 'image/png,image/gif,image/jpeg', id: 'pictureInput', class: 'controls') %>
        </div><br>

        <div class="form-group">
          <%= f.submit class: 'btn btn-primary' %>
        </div>
      </div>
      <% end %>
    </div>
  </div>

</article>
</div>
