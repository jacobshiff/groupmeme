
<div class >
  <div class="portfolio-wrapper">
    <div class="row">
      <h2>Trending at <%= @group.title %> <%= render partial: "sort", locals: {group: @group} %></h2>
    </div>

  <!-- Need to add in code that crops image so all gifs are same size -->
       <!-- @memes.in_groups_of(3).each do |meme_row|  -->
      <% @memes.in_groups_of(3).each do |meme_row| %>
        <div class="row-eq-height">
          <% meme_row.compact.each do |meme|  %>
            
            <div class="card film art">
              <%= link_to meme_path(meme.group.group_slug, meme), class: "thumb" do %>
                  <%= image_tag(meme.image.url, class: "img-thumbnail") %>
                  <span class="overlay"><span class="fa fa-search"></span></span>
              <% end %>

              <a href="portfolio-item.html" class="thumb">
                <img src="img/sample/sintel/sample-sintel-1.jpg" alt="" title="" />
                <span class="overlay"><span class="fa fa-search"></span></span>
              </a>
              <div class="card-body">
                <h2><a href="portfolio-item.html">Sintel at Seattle International Film Festival</a></h2>
                <p>The cinema where Sintel screens has digital 4k projection, I’m working with them getting this formatted right and delivered. Would be a real ...</p>
                <p><a href="portfolio-item.html">Read More</a></p>
              </div><!-- end card-body -->

            



            <div class="col-md-4">
              <div class="index-meme-container">
              <%= link_to image_tag(meme.image.url, class: "img-thumbnail"), meme_path(meme.group.group_slug, meme)%>
              <h5><small>posted by <%= meme.creator.username %></small></h5>
              <!-- Reactions div -->
                <div class="react" meme_parent_id="<%=meme.id%>">
                  <%= render partial: "memes/memes_reactions", locals: {meme: meme} %>
                </div>
              </div>
            </div>



          <% end %>
      </div>
      <% end %>
  </div>
</div>