<%= link_to react_path(id: meme.id), method: :post, remote: true do %>
  <%= ReactionDecorator.new(meme, current_user).thumb_html %>
<% end %>
<%= ReactionDecorator.new(meme, current_user).message_html %>
