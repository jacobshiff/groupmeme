<%= stylesheet_link_tag "http://fonts.googleapis.com/css?family=Roboto:400,100,300,500" %>
<h1>All Groups</h1>

<% @groups.each do |group| %>
  <%= link_to group.title, group_path(group.group_slug) %> <br>

<% end %>
